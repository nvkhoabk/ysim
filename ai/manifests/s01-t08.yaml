schema_version: "1.0"

project:
  code: YSIM
  version: "2.1"

sprint:
  code: S01
  baseline: architecture-v2.2

task:
  code: s01-t08
  order: 8
  name: "Frontend Shells, Design Tokens & Shared UI"
  status: planned
  execution_mode: sequential

depends_on:
  - s01-t07

objective: >
  Implement the frontend shells, design tokens & shared ui while preserving all
  architecture invariants and avoiding business-domain implementation.

scope:
  include:
    - "admin-web shell"
    - "storefront-web shell"
    - "design tokens"
    - "shared ui"
    - "runtime-context"

  exclude:
    - "Product"
    - "Pricing"
    - "Order"
    - "Payment"
    - "Allocation"
    - "Supplier Gateway"

quality_gates:
  - "git diff --check"
  - "ysf verify"

deliverables:
  - "Implementation"
  - "Task report"
  - "Validation evidence"

commit:
  message: "feat(s01-t08): Frontend Shells, Design Tokens & Shared UI"
